spring.application.name=WizardFrac
spring.profiles.active=dev

# Default profile: dev (H2 in-memory database)
# To use Supabase, set: spring.profiles.active=prod
# Or run: java -Dspring.profiles.active=prod -jar app.jar

# Environment variable loading via EnvConfig
# .env file will be loaded automatically if it exists
